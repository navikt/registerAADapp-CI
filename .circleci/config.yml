version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:10.0.0

    steps:
      - checkout

      - attach_workspace:
          at: .    

    docker:
      - image: northgrove/regsiteraadapp-ci
          

    
    #working_directory: github.com/navikt/iac

    #steps:#
    # - checkout

    
    #working_directory: /

    